# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - easybake-recipe-service

on:
  push:
    branches:
      - main
    paths:
      - 'services/recipe-service/**'
      - '.github/workflows/main_easybake-recipe-service.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: services/recipe-service/package-lock.json

      - name: 'npm install, build, and zip'
        env:
          DATABASE_URL: "postgresql://placeholder:placeholder@localhost:5432/placeholder"
        run: |
          cd services/recipe-service
          npm install
          npx prisma generate
          npm run build --if-present
          chmod -R +x ./node_modules/.bin
          # Zip the contents of the current directory into a file called release.zip
          zip -r ../../release.zip .

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: recipe-app
          path: release.zip # Upload the single zip file instead of the folder

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: recipe-app
          # No path needed here; it will download release.zip to the root
      
      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'easybake-recipe-service'
          slot-name: 'Production'
          # Point the package parameter directly to the zip file
          package: release.zip
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_D2B3E939DD6F44F9A9699799ACBB68A3 }}
